# AGENTS.md — cppstyle-profiles

cppstyle が消費する C++ コーディングスタイルプロファイルの正本。

## 規則

- プロファイル変更は cppstyle 側の検査で検証してからコミット。
- 検証済みの知見 (判断・手法・教訓) は本リポジトリに溜めず shiron へ ingest する (`shiron-ingest`)。
- 一次資料 (データシート等) は genten へ。本リポジトリには引用 ID で参照を置く。
